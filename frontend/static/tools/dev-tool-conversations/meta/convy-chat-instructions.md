## Step 0 — Click the project on the left menu

## Step 1 — Create the new chat

Use the message box : "Create a new canvas document and wait for me to paste the specification."

Send message: 

That message creates a brand new chat thread automatically. Do not send any other text.

Step 2 — Wait for empty canvas creation

I will create an empty canvas in that thread. Click Edit to make the canvas editable. You will see a blank document area.

step 3 - Paste the content of convy-creation-canvas.md to the canvas and click outside the canvas so it saves automatically

Step 4 - Now upload:

convy-master.png

convy-trimmed-example.png

And send this exact message:

The canvas is your authoritative Convy Asset Specification.

Attached:
- convy-master.png (strict visual identity reference)
- convy-trimmed-example.png (alpha-trim reference only)

Use them exactly as defined in the specification.
Confirm readiness only if everything is understood and locked.

Reply with exactly: Locked.

Step 5: Send message to create png

Example: "now Convy is measurements"


trim this image to spec and give me the sandbox link to the file name as you are instructed